<div class="page-header">
  <%= link_to new_power_unit_yellow_alarm_path(@power_unit), :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    New Alarm
  <% end %>
  <h1>Listing alarms</h1>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
            <th>Raw value</th>
            <th>State</th>
            <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @yellow_alarms) do |yellow_alarm| %>
            <td><%= yellow_alarm.raw_value %></td>
            <td><%= yellow_alarm.state %></td>
            <td><%= link_to 'Show', power_unit_yellow_alarm_path(@power_unit, yellow_alarm) %></td>
        <td><%= link_to 'Edit', edit_power_unit_yellow_alarm_path(@power_unit, yellow_alarm) %></td>
        <td><%= link_to 'Destroy', power_unit_yellow_alarm_path(@power_unit, yellow_alarm), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      <% end %>
    </tbody>
  </table>
</div>
