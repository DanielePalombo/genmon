<div class="page-header">
  <%= link_to power_unit_mixed_informations_path(@power_unit), :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to power_unit_mixed_information_path(@power_unit, @mixed_information), :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Show
  <% end %>
  <h1>Editing mixed_information</h1>
</div>

<%= render 'form' %>
